/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    unoptimized: true,
  },
  // trailingSlash: true,
  // basePath: "/leads-next",
  // assetPrefix: "/leads-next",
  // reactStrictMode: true,
};

module.exports = nextConfig;
